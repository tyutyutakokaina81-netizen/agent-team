# 夜間バグ調査レポート（正しさのバグ）
- 2026-06-30 夜間・調査エージェント／対象: note_publisher/*.py, gen_footers.py, tools/*.py, *.yml, *.mjs

## 🔴 重要1（修正済）: make_publish_pack.py 本文抽出が厳格すぎ→24記事で本文空の公開パックが無警告生成
- 原因: `##\s*本文\s*\n` が注釈付き見出し「## 本文（noteにそのままコピペ…）」を取りこぼし→body=""フォールバック・警告なし。タイトルは別regexで取れるため「正しいタイトル＋空本文」が生成。
- 修正: 抽出を `##\s*本文.*?\n` 寛容形へ統一（publish_to_note.py と同形）＋「**本文（…）:**」形式も対応＋空本文時 stderr 警告。
- 検証: 本文抽出 24空→0空（123/123 OK）。

## 🟡 重要2（修正済）: fetch_note_thumbnails.py タイトル抽出も厳格regex（112/124）
- 影響: stemフォールバックでクラッシュなしだがサムネ検索語の精度低下。修正: `.*?` 寛容形＋「**タイトル:**」対応。

## 🟡 重要3（報告のみ・要owner判断）: gen_footers.py classify() が registry 主カテゴリ「旅」を返さない
- registry内訳 旅8/食3/場所2/仕事1 だが classify は 食/場所/仕事 のみ→pick_related の同カテゴリ優先が「旅」に効かず関連選定が設計意図どおりでない。フォールバックで旅/場所→全体追加のためクラッシュ/重複なし（実害=関連性低下）。修正案: classifyに「旅」追加 or カテゴリ体系統一。owner分類意図次第で未修正。

## ⚪ 問題なし確認
make_epub.py / generate_thumbnails.py（provenance/リトライ/サイズ検証/gitignore+add -f）/ fetch_thumbnails_picsum / fetch_toyama_photos / generate_share_thumbs / tools(jobs,retainers,quality_check※コードブロック誤検出のみ要確認,collect_checks,cw_fetch,set_affiliate_id,make_etsy_products)/ 全ワークフロー。

## 朝の対応候補（任意・¥0）
- 重要3 classifyに「旅」追加（owner方針確認後）。quality_check のコードブロック誤検出ガード（軽微）。
