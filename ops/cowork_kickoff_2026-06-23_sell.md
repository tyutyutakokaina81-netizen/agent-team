# cowork 起動指示（2026-06-23 売る編）— そのまま cowork に渡す

あなた＝**cowork（副・実行担当・ネット可）**。canonical = `main`。判断はせず、下記の発注を**実行だけ**する。
code(私)は外部ネット不可(A1)＝制作担当。**"売る・出す・送る"はあなたの役目。**

## 手順
1. 最新取得：`git checkout main && git pull origin main`
2. 自分宛を確認：`python3 ops/process_inbox.py list --to cowork`
3. 下表を**優先順に実行**（各 `ops/inbox/*.yaml` 本文の指示どおり）。
4. 着手＝`take`、完了＝`done`、結果は `ops/outbox/` に報告 → `git add -A && git commit && git push origin main`。

| 優先 | ID | 内容 | 最初の一歩 |
|---|----|------|-----------|
| 1 | 2026-06-23_005 | **売る実行**（高単価B2B営業＋近場の現金＋Vol.1送客） | 見込み事業者に提案＋お試しサンプルを週20〜40件接触。受注→code連携 |
| 2 | 2026-06-23_004 | **7日で1円**（既販売Vol.1へ送客） | X投稿1日1本＋既存note3記事にCTA。Day2のみリンク |
| 3 | 2026-06-23_003 | **Kindle6冊＋Gumroad入稿** | ※オーナーのKDP登録が済んでから。EPUBは repo にある |

## 着手条件（重要）
- オーナーのアカウント（メール/SNS/note/クラウドソーシング/KDP）に**ログインして操作できること**。
- できない項目があれば、止めて `ops/outbox/` に「○○のアクセス不可・要オーナー設定」と報告。

## 厳守
- ゼロ予算（広告課金しない）。A4 PII禁止。A5 誇張・成果保証NG。スパム/規約違反NG。
- 公開・送付は名指し分のみ・一括無検証NG・未来日付/テスト/重複は出さない。1日最大5本。
- 受注/発注/反応は**必ず outbox に報告**。code がそれを見て即制作する。

## 売れたら
- 受注内容を outbox に書く → code が成果物を即制作 → あなたが納品 → 入金を `tools/jobs.py done` で記録。

> 迷ったら止めて outbox に質問。canonical(main) を壊さない。**作る側(code)は待機完了。動かすのはあなた。**
