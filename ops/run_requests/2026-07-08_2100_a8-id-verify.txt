要求: A8 a8mat照合（owner「A8登録してます」2026-07-08）＝収益バルブ1の点検・最優先
発行: code 2026-07-08

目的: 全152箇所に埋め込まれているA8アフィリID(a8mat)が owner本人のものか確認する。
  別人のIDなら今のクリック報酬が他人に漏れている＝最大の穴。owner本人なら即OK。

★絶対制約（機密）:
- **owner認証情報(ID/パスワード)は絶対に打鍵しない・受け取らない・保存しない**（note 2FAルールと同じ）。
- a8mat は「公開の追跡コード」＝リンクURLに載る非機密値。読むのはこれだけ。

手順:
1. owner のブラウザ(ログイン済みセッション)で a8.net を開く。
2. **もしログイン画面が出る/未ログインなら → 直ちに停止**。認証情報を打たず、outbox に「a8未ログイン＝owner本人が一度ログイン要」と1行報告して終了。
3. ログイン済みなら「プログラム管理 > 参加中プログラム」→ **じゃらん** の広告リンク(基本リンク/バナー)を開き、URL内の `a8mat=◯◯◯◯` の値を読む。
4. **楽天** も同様に `a8mat=` を読む。
5. 埋込値と照合し outbox に報告:
   - じゃらん 埋込 = `1CCUH0+929KAA+14CS+64RJ5`
   - 楽天　　 埋込 = `1U7G8Q+5LTYUQ+2HOM+686ZL`
   - 一致 → 「両方一致=owner本人・差替え不要」。
   - 不一致/一方だけ → **正しいa8mat値(公開コードのみ)を報告**。code が `python3 tools/set_affiliate_id.py --jalan/--rakuten <値> --apply` で全152箇所を差替え&push。
6. ブラウザは headless/後始末（owner画面に残骸を残さない）。

制約: ¥0・認証情報は打鍵/受領/保存しない・読むのはa8mat公開コードのみ・A5。
