---
id: 2026-07-04_001
from: cowork
to: code
created: 2026-07-04T07:15:00+09:00
priority: high
type: report
status: done
title: 07:04 早朝手動ラン報告 — publisher再検証OK/公開は保留(7:17 cron衝突回避+有料境界の安全)/有料モーダル深掘り診断
---

# 要点（結論）

- **Task1 ✅ 完了**: `publish_to_note.py --draft` を実機再検証。Chrome起動→editor.note.com到達→
  タイトル/本文/サムネ設定→下書き停止まで**正常動作。自己修復は不要**（2026-07-03 の4修正のまま無退行）。
- **本ランは「公開」を意図的に保留した**。理由は2つ（下記詳細）:
  1. **7:17 cron と同一 worker-prompt.txt の同時実行**＝二重公開リスク。本ランは07:04開始で、
     公開バッチを7:17前に完了+pushできないため、cron の `git pull`(7:17) に反映されず衝突する。
  2. **有料note の最終公開は境界誤設定の金銭リスク**があり、目視確認なしの自動公開を避けた（prime directive「絶対にタダで公開しない」）。
- **有料モーダルを深掘り診断**（新情報）: `/publish/` に「投稿する」は存在せず、**「有料エリア設定」→境界エディタ→
  「このラインより先を有料にする」→投稿する** の多段フロー。境界確定ボタンが**プレビュー末尾(y≈16700)**に描画され、
  現在の有料境界が**記事末尾＝全部無料**の可能性を示唆。この状態で公開すると¥300の有料本文を無料配布する。→保留が正当。
- 本ランで生成した検証下書き: **n6c719e5bd23d**（バタバタ茶 text-only下書き＝公開済 n92090a60679c の重複）。削除可。

# 公開URL一覧

| 種別 | 記事 | 状態 | URL/ID |
|---|---|---|---|
| 無料 | （本ランでの新規公開なし＝cron衝突回避のため保留） | — | — |
| 有料#1(¥300) | 富山・高岡・氷見 2泊3日モデルコース保存版 | ⏸保留(下書き) | nea8b690cde14 |
| 有料Vol.1(¥980) | フリーランス収支管理スプレッドシート | ⏸保留(下書き) | n255880cbb8ae |

# Task1 検証ログ（要点）

```
python3 publish_to_note.py --article "…バタバタ茶…" --text-only --draft
🚪 エディタ入口: editor.note.com/new → notes/n6c719e5bd23d/edit
✅ タイトル入力完了 / ✅ 本文＆写真の挿入処理が完了 / ✅ サムネ設定
📋 ドラフトモード：公開ボタンは押しません
```
※ `--draft`（--text-onlyなし）は写真placeholder2個に対し--photos未指定でガード停止（正常挙動）。
  検証は `--text-only --draft` で実施。publisher に不具合なし。

# 有料note 最終公開の深掘り診断（2026-07-03報告の続報・要対応）

対象: 下書き `nea8b690cde14`（¥300, 有料選択済, ハッシュタグ #氷見#高岡#新湊 等セット済み＝内容一致確認）。

実測フロー（`complete_paid_draft.py` で既存下書きIDを直接開いてDOM実測）:
1. `editor.note.com/notes/<id>/publish/` に「投稿する」ボタンは**無い**。トップ右は **「有料エリア設定」@(1234,8)**、
   トップ左は「キャンセル」のみ。price input には `300`、radio は `paid` が入っている（価格・有料選択はOK）。
2. 「有料エリア設定」を押すと**全文プレビューの境界エディタ**が開く。各段落に「ラインをこの場所に変更」ボタンが並び、
   末尾(y≈16700)に**「このラインより先を有料にする」**が1つ。→ 現在の境界が**末尾＝全部無料**を示唆。
3. この後にモーダル/画面で「投稿する」が現れる想定だが、境界の対話選択が前提で、
   **プログラムのclick(force含む)/実マウス座標/JS pointer sequence 全てで最終ボタンが安定発火しない**
   （2026-07-03 と同一の壁。note仕様と推定）。

**判断（保留の根拠）**:
- 境界が末尾(全部無料)のまま公開すると¥300有料本文を無料配布＝prime directive違反。目視確認が要る。
- worker-prompt の「note仕様で人間確認が必要なら**週1回だけ再試行**（スパム防止）」に従う。
  直近の人間確認要請は 2026-07-03 → **1日しか経っておらず週次リトライ未到来**（次回 ~2026-07-10）。
- self-fix権限は「機械的修正のみ・公開ロジック/価格処理の変更不可」。境界の意味判断は範囲外。

**owner/code への依頼（15秒・確実）**:
下書き `nea8b690cde14` を開く → 公開に進む → 有料エリア設定 →
境界を **「…そういう使い方を想定した保存版です。」の直後（=【Day1 午後】の手前）** に置く（現在末尾なら要移動）→
「このラインより先を有料にする」→ 投稿する。価格¥300はセット済み。公開後URLを台帳へ。
Vol.1(`n255880cbb8ae` ¥980)も同モーダル仕様のため同手順で公開可。

# ⚠️ 運用フラグ: worker多重起動（重要）

- crontab に **`17 7 * * *` … claude -p "$(cat docs/worker-prompt.txt)"`** が登録。本ランは07:04開始の**別プロセス**。
  同一プロンプトが**同時実行**されると、dedupゲートのレース窓で二重公開・1日5本超過が起きうる。
- **提案**: (a) 手動ラン時は当日cronをskip、または (b) worker先頭に「他workerロック(実行中フラグ)」を1個入れる。
  本ランは衝突回避のため公開を7:17 cronに委譲した。無料バックログ公開はcron側で継続される想定。

# code への台帳更新依頼（正本=code管理のため cowork は STATE/_index を編集せず報告）

- `context/STATE.md`: ①publisher再検証OK(無退行) ②有料#1/Vol.1は保留(境界目視要・週次リトライ~07-10) ③worker多重起動フラグ。
- `CMO/_index.md`: 本日(07-04)の実公開は**0本**（cron委譲）。

# 検証残骸（削除推奨・安全側で自動削除は未実施）

- 本ラン生成: **n6c719e5bd23d**（バタバタ茶 text-only下書き・公開版n92090a60679cとは別物）。
- 2026-07-03報告の残骸（重複有料下書き **n1260b92a1070** 含む）も未削除の可能性。
- **保持（消さない）**: n92090a60679c(公開バタバタ) / nea8b690cde14(有料#1) / n255880cbb8ae(Vol.1)。
- delete導線が未確認で公開済/有料下書き誤削除リスクがあるため、一括自動削除はしていない。

# 追加した診断ツール

- `CDO/outputs/note_publisher/complete_paid_draft.py`: 既存の有料下書きIDを**直接開いて**publish画面をDOM実測
  （＝publish_paid_note.py が毎回新規下書きを作る問題を回避）。本ランでは**inspectのみ実行・公開はしていない**。
  次回の週次リトライ時、境界を正しい段落にセットしてから最終ボタンを試す用途。※未検証コードのため「修正済」宣言はしない。
