# cowork 起動指示（2026-06-23）— そのまま cowork に渡す

あなた＝**cowork（副・実行担当）**。canonical = `main`。判断はせず、下記の発注を**実行だけ**する。

## 手順
1. リポジトリで最新を取得：`git checkout main && git pull origin main`
2. 自分宛の未処理を確認：`python3 ops/process_inbox.py list --to cowork`
3. 下表の open タスクを**優先順に実行**（各 `ops/inbox/*.yaml` 本文の指示に従う）。

| 優先 | ID | 内容 | 要点 |
|---|----|------|------|
| 1 | 2026-06-22_001 | note棚卸し（**公開しない・読取のみ**） | 重複/テスト/未来日付/サムネ未設定を点検して報告 |
| 2 | 2026-06-23_002 | サムネ一括（やさしいイラスト） | `research/33` の検索ワードで「みんなのフォトギャラリー」(無料) → サムネ未設定のエッセイ記事に付与 |
| 3 | 2026-06-22_004 | [要確認]138件 ネット検証 | `research/31` を ✅確実/⚠️要修正/❓諸説あり で判定し根拠を添える |
| 4 | 2026-06-22_003 | 1週間スプリント投稿 | `research/28` の完成文をそのまま（CTA3記事/橋渡し記事/X7本・リンクはDay2の1回） |
| 5 | 2026-06-22_005 | 写真生成（食特集14枚） | `research/32`、写真風・無料手段のみ |

## 厳守（全タスク共通）
- **無料手段のみ**（有料API課金しない＝ゼロ予算）。
- **A4 PII禁止**（地区名・町名・住所・実在店名・人名を出さない）。誇張・断定NG。
- 公開系は **名指し分のみ・一括公開しない・未来日付/テスト/重複は出さない**（2026-06-12 インシデント再発防止）。**1日最大5本**。
- 一文でも怪しければ**保留して outbox に報告**（出してから直すより安全）。

## 完了後
- 各タスクを `take` → 実行 → `done`（`processed/` へ移動）。
- 結果を `ops/outbox/<日付>_NNN_cowork_code.yaml` に報告 → `git add -A && git commit && git push origin main`。

> 迷ったら実行を止めて outbox に質問を残す。canonical(main) を壊さない。
