# STATE.md — 記憶・最新状態（永続メモリ・軽量版）

> このファイルは **セッション開始時に SessionStart フックが自動で読み込む**。タスク開始前に必ず最初に読む。
> コンテナは使い捨て。**記憶はここに書いて commit & push しない限り消える。**
>
> ⚠️ **このファイルは軽量に保つこと（目安 15KB 未満）。** 肥大化するとフック出力が truncate され、
> リセット（新コンテナ）のたびに記憶がロードされなくなる（2026-07-15 に実際に発生・修正済）。
> **過去の詳細な経緯・全決定ログは `context/STATE_ARCHIVE.md` に退避してある**（消していない）。
> 古い項目は STATE.md に貯めず、要約1行を残して詳細は ARCHIVE へ送る運用。

最終更新: 2026-07-15（★記憶システムの補強：肥大化(73KB)で毎回 truncate され記憶が実質ロードされていなかった事故を修正。全履歴を STATE_ARCHIVE.md へ退避し本ファイルを軽量化。フックに肥大化ガードを追加。）

---

## 0. 北極星（North Star・最優先）

**海外読者に 高岡・氷見・富山 の記事を読んでもらう。**（AGENTS.md §0）
儲けは本命にしない＝「狙う」でなく「来たら拾う」（2026-06-22 owner確定）。ゼロ予算・赤字にしない。

---

## 1. 絶対制約（A1〜A7・毎セッション厳守）

| # | 制約 | 補足 |
|---|---|---|
| A1 | **外部ネット全面ブロック** | note.com・Google 等すべて403。code からの直接投稿/取得/閲覧は不可（※WebSearch は proxy 経由で可・WebFetch は403） |
| A2 | **画像生成不可（code 側）** | 画像生成AIなし。code はサムネを作らない |
| A3 | **サムネはオーナー/cloudが取得（写真風で統一）** | code は記事に `[写真X]` placeholder を置くだけ |
| A4 | **PII禁止** | 具体的な地区名・町名・住所は一切書かない（市レベルまでOK） |
| A5 | **テンプレ・適当・誇張NG** | 「Xには、Yがある」連発／同日内重複／「世界唯一」断定 禁止 |
| A6 | **同パターンの英語要約2連続NG** | "If you make things..." 型反復禁止 |
| A7 | **コンテナは使い捨て** | commit & push しない限り消える。STATE.md記録＋push必須 |

---

## 2. 「実行して」プロトコル v2（承認不要で自動実行）

owner が「実行して／実行／今日の分」と言ったら承認不要で：
1. STATE.md＋CMO/_index.md を読む → 5本揃っていない最も近い日付を対象に
2. **note記事を5本厳守**（多くも少なくもしない）。**5本中2本は食**。同一カテゴリ2日連続回避。高岡住人の一人称。
3. 各記事：**2000字**・**みんなのフォトギャラリー検索ワード**でサムネ指定・**英語要約併記（毎回別パターン）**・末尾に**「事実検証ノート」**（✅確実/⚠️要確認/諸説あり）。交通/運賃/パス系は WebSearch で裏取り。
4. 保存 `CMO/outputs/YYYY-MM-DD_note記事_*.md`（英語版 `CMO/outputs/EN/`・素材 `EN/outputs/`）＋クロスポスト素材生成。
5. `CMO/_index.md` と本 STATE.md を更新 → **無確認で commit & push** → 報告は要点のみ（B11）。

> 公開（実投稿）は A1 で code 不可 → owner/cowork。写真あり `prepare_photos.py --then-publish`／写真なし `publish_to_note.py --text-only`。

---

## 3. 現在の最新状態（2026-07 時点）

**確定方針＝完全放置(HP/SEO)モデル**（2026-06-24）。この事業の天井は「owner が外部で何アクションするか」だけで決まる。code は制作無限だが A1 で外部ゼロ。過去¥0の真因は能力でなく外部1アクションの欠如。
- **code の仕事**：高購買意欲の SEO 記事／多言語ページを静かに足し続ける（量でなく質）。
- **owner の仕事**：基本ゼロ。問い合わせが Gmail に来た時だけ文面を code に貼る→返信案を作る。

**公開サイト（本番・GitHub Pages で世界公開中）**：
- `apps/toyama-guide/`＝富山・高岡・氷見の海外向けガイド。**英語44本＋概要ハブ6言語**（中[簡/繁]・韓・仏・独・西）。全体で約91ページ。hreflang 相互リンク・sitemap・FAQ構造化データ完備。
- `apps/ai-agency-hp/`＝AI自動化代行LP（日/英）。Google Search Console 所有権確認済（`google4a0bf4b0a669a11d.html` は**削除しない**）。
- **全自動パイプライン（owner作業ゼロ）**：main マージ → ①Pages 世界公開 ②IndexNow で検索エンジン自動通知＋sitemap更新 ③`toyama-photos.yml` が og:image 写真を全ページ自動生成（`fetch_toyama_photos.py`・未登録ページは汎用語フォールバックで穴埋め）。
- ⚠️ **公開＝main マージが必須**。作業ブランチに積むだけでは世界に出ない（2026-06-26 の重大教訓）。

**収益配線**：じゃらんA8（現ID `1CCUH0+929KAA+14CS+64RJ5`）・楽天（`1U7G8Q+5LTYUQ+2HOM+686ZL`）を全ページ設置済。空ボタン0。`tools/set_affiliate_id.py` で owner の正ID を一括置換可。
**到達計測**：GA4 タグを全ページ配線済（`analytics.js`）。**owner が測定ID(G-)を渡せば計測開始**（未投入）。

**収益の現況＝0→1 未達**：実入金はまだ（「入金先設定」は済＝出口は準備OK）。無収益の律速＝①読者がまだ来ていない ②A8 ID が owner本人のものか未確認（A8画面は owner しか見えない）。

---

## 4. owner の未了アクション（ここが律速・code は A1 で不可）

1. **GA4 測定ID(G-)** を code に渡す（→次デプロイで到達が数字で見える）。
2. **A8 で じゃらん/楽天トラベル の ID を確認**し、本人IDなら OK・違えば正IDを code へ（→`set_affiliate_id.py` で一括反映）。楽天トラベル未提携なら1クリック提携で海外予約収益がオンに。
3. **SNS/note への実投稿**（配信キット `EN/outputs/pinterest/`・`EN/outputs/reddit/` 準備済）or cowork 発注。

---

## 5. 次にやること（次回着手候補）

- **多言語拡大の続き**：仏独西を概要1本から主要記事へ拡張／中韓で雨晴・アルペン等の個別記事を増やす。
- **OG画像整備**：主要ガイドの og:image が SNS 共有時のサムネ＝CTR の要（`toyama-photos.yml` で自動生成中・穴が残れば QUERY 追記）。
- **note記事の継続制作**（2000字＋みんフォト＋事実検証ノートの標準書式）。
- 「実行して」発令時＝§2 プロトコルで note 5本。

---

## 6. code × cowork × owner の役割

- **code（主・頭脳）**：企画・執筆・整理・分析・台帳/STATE 正本管理・git/PR・公開パイプライン設計。`main` が canonical。
- **cowork（副・ミラー）**：外部ネット/ブラウザ/セッション認証/定期実行/並列バッチの**実行だけ**代行。企画・新規ブランチ作成はしない。cowork宛の変更は必ず `main` に反映（inbox にcommitだけでは届かない）。
- **外部ワーカー**：`worker/<name>-*` ブランチで作業→code がレビューして main へ収束。
- **ops/ キュー**：`python3 ops/process_inbox.py list --to code` で自分宛を確認。inbox=code→cowork、outbox=cowork→code。

---

## 7. 直近の重要マイルストーン（要約・詳細は STATE_ARCHIVE.md）

- 2026-07-15：★記憶システム補強（本ファイル軽量化・肥大化ガード追加）。
- 2026-06-26：多言語6言語ハブ本番公開／サムネ自動連携（フォールバック）／過去35ページのサムネQUERY修正／事実誤り高優先5件を全ソース訂正／note記事(2000字)量産／PR #54 main マージで44ページ世界公開が実際に発火。
- 2026-06-25：GA4 到達計測を全91ページに配線（owner の測定ID待ち）／収益リンク監査＝配線は完備。
- 2026-06-24：★方針確定＝完全放置(HP/SEO)モデル・ゼロ予算・KPIは「初の問い合わせ(Gmail)」1つ／Search Console 所有権確認完了／SEO pillar-cluster を日英で完成。
- 2026-06-22：★会社方針確定＝本命は発信・儲けは来たら拾う・お金かけない（owner決裁）。
- 2026-06-24：★★初の外部配信（note公開）＝0→1 の最初の一歩（実入金はまだ）。

> ⚠️ 2026-06-12 インシデント（note 重複/テスト/未来日付公開）以降、**自動公開は棚卸し完了まで慎重運用**（1日最大5本・未来日付ガード・重複チェック）。

---

## 8. 決定ログ（新しいものだけ・古いものは STATE_ARCHIVE.md §決定ログ）

| 日付 | 決定事項 |
|------|---------|
| 2026-07-15 | **記憶(STATE.md)が73KBに肥大化し、SessionStart フックの出力上限超過で毎回 truncate＝リセットのたびに記憶が実質ロードされない事故が判明。** 全履歴を `context/STATE_ARCHIVE.md` へ退避（削除せず）、STATE.md を軽量な現行メモリに再構成、フックに肥大化警告ガードを追加して再発防止。以後 STATE.md は 15KB 未満を維持し、古い経緯は要約1行＋詳細は ARCHIVE へ送る運用。 |
| 2026-06-24 | 方針確定＝完全放置(HP/SEO)モデル。owner 外部アクション≒ゼロ、code は SEO記事を静かに追加。KPI＝初の問い合わせ(Gmail)1つ。¥300K目標は撤回・儲けは来たら拾う。 |
| 2026-05-28 | 「実行して」＝今日の note 記事作成。完了時は無確認で自動 commit & push。無人スケジュールはしない（オンデマンドのみ）。 |

---

> 過去の全経緯（多言語拡大の詳細・収益化監査・0→1 の推移・受託/AI代行の試行錯誤・全決定ログ）は
> **`context/STATE_ARCHIVE.md`** を参照。本ファイルは「今このプロジェクトで動くための最小の記憶」に保つ。
