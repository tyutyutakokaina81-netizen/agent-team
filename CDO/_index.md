# CDO インデックス（技術責任者）

## 担当業務
- プロンプト作成・管理・改善
- Claude Code活用・自動化
- 技術検証・PoC
- ツール整備・ワークフロー改善
- 新役職フォルダの自動生成

> **ルール：** ファイルを作成・更新するたびに必ず下の成果物ログに追記すること。

## 成果物ログ

| 日付 | ファイル名 | 種別 | 概要 | ステータス |
|------|-----------|------|------|-----------|
| 2026-05-06 | projects/2026-04-08_月30万自動化/D_エクセル入力スクレイピング/pipeline/README.md | ガイド | 柱D パイプラインの操作ガイド。ローカル/ヘッドレス分担・OS別セットアップ・トラブルシュート | 完了 |
| 2026-05-06 | projects/2026-04-08_月30万自動化/D_エクセル入力スクレイピング/pipeline/03_apply.py | 修正 | `j.get("recommend")` → `j.get("verdict") in ("GO","CAUTION")` に修正（評価結果のキー名不一致でフィルタが常に空配列を返す不具合） | 完了 |
| 2026-05-06 | projects/2026-04-08_月30万自動化/D_エクセル入力スクレイピング/pipeline/run_pipeline.py | 修正 | `webbrowser.open` をヘッドレス耐性化（`_open_url` ラッパー追加。`DISPLAY` 未設定の Linux ではスキップ） | 完了 |
| 2026-05-06 | .gitignore | 設定 | 柱D の `outputs/.gitkeep` `.sessions/.gitkeep` 例外を追加してフォルダ構造を維持 | 完了 |

## 進行中タスク

- （なし）

## メモ・引き継ぎ事項

- 柱D パイプライン：`00_session_setup.py` のみオーナーのローカル PC で実行が必要（GUI ブラウザと対話入力が必須）。それ以外（`01_*.py`〜`06_*.py`、`run_pipeline.py`）はヘッドレスでも動く設計。
- Playwright Chromium のダウンロード（`cdn.playwright.dev`）はサンドボックス環境では allowlist 外で 403 になるケースを確認。費用ゼロで対処するなら「ローカルでセッション作成 → サーバー側に `.sessions/*.json` を持ち込み」が最短経路。詳細は pipeline/README.md のトラブルシューティング欄。
